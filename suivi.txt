Delete clé étrangère Contrainte
Gestion des erreurs

TSLint :
coommande : ./node_modules/.bin/tslint -c tslint.json 'src/**/*.ts'

Deployment :
Chemin : C:\Users\naelb\WebstormProjects\GSB\dist\gsbfrais-client\browser

Script ((post-commit)PAS D'EXTENSION !!)

#!/bin/bash

# Récupère la branche actuelle
branch=$(git rev-parse --abbrev-ref HEAD)

# Déclenche le déploiement uniquement si vous êtes sur la branche principale (par exemple, 'main' ou 'master')
if [ "$branch" == "main" ] || [ "$branch" == "master" ]; then
    # Accédez au répertoire de votre projet Angular
    cd C:/Users/naelb/WebstormProjects/GSB

    # Effectuez les actions nécessaires pour déployer votre projet
    # Par exemple, si vous utilisez Angular CLI :
    ng build

    # Ajoutez ici toute autre étape de déploiement nécessaire

fi
